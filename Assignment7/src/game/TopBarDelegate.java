package game;

//Instead of performing one of object¡¦s stated tasks, delegates that task to an associated helper object.

public interface TopBarDelegate {
	public void setLabel(int rockCount,float height);
}
